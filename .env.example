PORT=3000
NODE_ENV=development

# LLM Configuration
# Modes available: "local" (zero API dependency) | "external"
LLM_PROVIDER_MODE=local

# Optional: Add keys when switching mode to "external"
CLAUDE_API_KEY=
GEMINI_API_KEY=
