version: '3.8'

services:
  risk-engine-api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - LLM_PROVIDER_MODE=local
    restart: unless-stopped
